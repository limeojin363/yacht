import { z } from "zod";

export const EnterTheRoomReqBodySchema = z.object({});

export const EnterTheRoomResBodySchema = z.object({});

export const ExitTheRoomReqBodySchema = z.object({});

export const ExitTheRoomResBodySchema = z.object({});
