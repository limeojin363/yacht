import S from "./styles";
import type { HandUnit } from "./types";

type SingleFieldScoreCellProps = {
  handUnit: HandUnit;
};


export const SingleFieldScoreCell = ({
  handUnit,
}: SingleFieldScoreCellProps) => {
  return <S.Root>{/* FILL HERE */}</S.Root>;
};
