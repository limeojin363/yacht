import styled from "@emotion/styled";

type MainColumnProps = {
  playerIdx: number;
};

const MainColumn = ({ playerIdx }: MainColumnProps) => {
  return <S.Root>{/* FILL HERE */}</S.Root>;
};

const S = {
  Root: styled.div`
    /* FILL HERE */
  `,
};

export default MainColumn;
