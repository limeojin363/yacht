import styled from "@emotion/styled";

type DicePickerProps = {
  select: (eye: number) => void;
};

const DicePicker = ({ select }: DicePickerProps) => {
  return <S.Root>{/* FILL HERE */}</S.Root>;
};

const S = {
  Root: styled.div`
    /* FILL HERE */
  `,
};

export default DicePicker;
