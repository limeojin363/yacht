import styled from "@emotion/styled"

// Context에 의존성을 갖는다

// 헤더에 들어갈거
// 좌측: 홈버튼
// 우측: 유저 정보(로그인, 회원가입)
const Header = () => {
    return <S.Root>{/* FILL HERE */}</S.Root>;
};

const S = {
    Root: styled.div`
        /* FILL HERE */
    `,
}

export default Header;
